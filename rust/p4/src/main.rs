fn main() {
    use std::process::Command;

   // let mut fname0 = "~/Documents/thing.rs";

    Command::new("echo")
        .arg()
       .spawn()
        .expect("command failed");    
      //  .arg("pleb")
      //  .arg(">>")
      //  .arg(fname0)
 
}
